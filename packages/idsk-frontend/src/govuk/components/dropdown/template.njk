{% from "../../macros/attributes.njk" import govukAttributes -%}

<div class="govuk-width-container">
  <div data-module="govuk-dropdown" class="idsk-dropdown__wrapper {%- if params.classes %} {{ params.classes }}{% endif %}"
    {{- govukAttributes(params.attributes) }}>
    <button class="idsk-button idsk-dropdown" type="button">
      <span>{% if params.selected %}{{params.selected}}{% else %}slovenčina{% endif %}</span>
      <svg width="1em" height="1em" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" class="idsk-dropdown__icon"><path d="m8.71 11.71 2.59 2.59c.39.39 1.02.39 1.41 0l2.59-2.59c.63-.63.18-1.71-.71-1.71H9.41c-.89 0-1.33 1.08-.7 1.71Z" fill="currentColor"></path></svg>
    </button>
    <ul id="undefined-dropdown" class="idsk-dropdown__options idsk-dropdown__options--down" data-testid="dropdown-options">
      {% if params.items | length %}
        {% for item in params.items %}
          <li class="idsk-dropdown__option idsk-pseudolabel__wrapper" data-pseudolabel="{{ item.text }}">
            <a href="{{ item.href }}">
              {{ item.text }}
            </a>
          </li>
        {% endfor %}
      {% else %}
        <li class="idsk-dropdown__option idsk-pseudolabel__wrapper" data-pseudolabel="english">
          <a href="#">english</a>
        </li>
        <li class="idsk-dropdown__option idsk-pseudolabel__wrapper" data-pseudolabel="український">
          <a href="#">український</a>
        </li>
      {% endif %}
    </ul>
  </div>
</div>
